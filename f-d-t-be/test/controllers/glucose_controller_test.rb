require "test_helper"

describe NameController do
  it "should get [action" do
    get name_[action_url
    value(response).must_be :success?
  end

  it "should get action]" do
    get name_action]_url
    value(response).must_be :success?
  end

  it "should get [options]" do
    get name_[options]_url
    value(response).must_be :success?
  end

end
